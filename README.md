# piccolo_project

## Setup

### build the containers

```bash
docker-compose build
```
### run the server

```bash
docker-compose up
```


### Running tests

```bash
docker-compose run web sh -c "piccolo tester run"
```
